// const { buildMutualGroups } = require("../utils/groupGraph");

// const getValidMutualGroups = async (req, res) => {
//   const collegeId = req.user.college;
//   const groups = await buildMutualGroups(collegeId);
//   res.json(groups);
// };
